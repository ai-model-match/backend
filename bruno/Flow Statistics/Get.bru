meta {
  name: Get
  type: http
  seq: 1
}

get {
  url: http://127.0.0.1:8001/api/v1/flows/{{firstFlowId}}/flow-statistics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstFlowStatisticsId", res.body?.item?.id);
  
}

settings {
  encodeUrl: true
}
