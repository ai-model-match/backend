meta {
  name: List
  type: http
  seq: 1
}

get {
  url: http://127.0.0.1:8001/api/v1/flow-steps?flowId={{firstFlowId}}&page=1&pageSize=10
  body: none
  auth: bearer
}

params:query {
  flowId: {{firstFlowId}}
  page: 1
  pageSize: 10
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstFlowStepId", res.body?.items[0]?.id);
  
}

settings {
  encodeUrl: true
}
